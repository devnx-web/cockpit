node_modules/
*.log
.DS_Store
*.swp
.cache/
dist/
build/

# Assets de terceiros copiados de node_modules pelo postinstall
public/vendor/

# Logs do Playwright MCP
.playwright-mcp/

# Config local do daemon de voz (paths absolutos do ambiente)
config.json

# Chave da API Groq (STT do ditado) — nunca commitar
.groq-key

# Estados transitórios de ferramentas de planejamento/brainstorm
.planning/debug/
.superpowers/brainstorm/

# agente de voz (experimento) — segredos e venv
integrations/voice-agent/.env.local
integrations/voice-agent/.venv/

# estado de runtime quando o servidor roda a partir do repo
demands.json
