[project]
name = "cockpit-voice-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "livekit-agents[mcp,openai]>=1.6.8",
    "python-dotenv>=1.2.2",
    "httpx>=0.27",
]
